import { WhiteSpaceValidation } from './white-space-validation';

describe('WhiteSpaceValidation', () => {
  it('should create an instance', () => {
    expect(new WhiteSpaceValidation()).toBeTruthy();
  });
});
